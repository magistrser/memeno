import {
    AddMem,
    RateMem,
    RemoveMem,
} from '../../../server/engine/IEngine/IMemesEngine';

export type AddMemReq = AddMem;
export type RateMemReq = RateMem;
export type RemoveMemReq = RemoveMem;
